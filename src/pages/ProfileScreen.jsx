import styled from "styled-components"

const ProfileScreen = () => {
  return (
    <ProfileScreenContainer>ProfileScreen</ProfileScreenContainer>
  )
}

export default ProfileScreen

const ProfileScreenContainer = styled.main``